# ml-projects
# Python Projects (Notebooks + Scripts)

This repository contains several independent Python projects (GUI + CLI + data processing).

## Contents

### 1) `budget_optimizer.py` — Budget Optimizer (Dynamic Programming / Knapsack)
A budget allocation tool that selects the best set of items under a fixed budget using dynamic programming (0/1 knapsack).

**Features**
- CLI (terminal menu)
- GUI (Tkinter)
- Optional visualization (matplotlib) when available
- Can load initial items from `products.json` (optional)

**Run**
- CLI:
  ```bash
  python src/budget_optimizer.py
